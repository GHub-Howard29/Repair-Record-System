{
  "name": "repair-record-system-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check tools/create-drive-refresh-token.mjs",
    "runtime:check": "node -e \"if (process.versions.node.split('.')[0] !== '22') { console.error('需要 Node.js 22；目前是 ' + process.version); process.exit(1) }\"",
    "drive:token": "node tools/create-drive-refresh-token.mjs"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^173.0.0"
  }
}
