# 06_外部資源連結設定

## 目的

本文件記錄維修紀錄系統需要的外部資源、建立順序、環境變數與驗證方式。後續串接 Firestore、Google Drive、GitHub Pages 時，依本文件逐項完成。

---

## 外部資源總覽

| 順序 | 外部資源 | 用途 | 目前狀態 |
|------|----------|------|----------|
| 1 | Google OAuth Web Client | 使用者登入 | 已完成本機登入驗證 |
| 2 | GitHub Pages | 前端部署 | 已完成實際網站與 icon 更新驗證 |
| 3 | Firebase / Firestore | 維修文字資料同步 | service 已串接，登入、寫入與跨電腦讀取已實機驗證 |
| 4 | Google Drive API | 照片附件上傳與保存 | 後端代理已部署，照片上傳已實機驗證 |
| 5 | GitHub Actions 或部署指令 | 自動 build / deploy | 已建立 workflow 與本機檢查指令 |

---

## 1. Google OAuth Web Client

OAuth Client 類型應選：

```text
Web application
```

`.env` 欄位：

```env
VITE_GOOGLE_CLIENT_ID=你的ClientID.apps.googleusercontent.com
```

已授權的 JavaScript 來源：

```text
http://127.0.0.1:5173
http://localhost:5173
https://repair-record-system.firebaseapp.com
https://ghub-howard29.github.io
```

注意：

- GitHub Pages 專案頁只填 origin，不填 `/Repair-Record-System/` path。
- 目前不需要填已授權的重新導向 URI。
- `.env` 不可提交到 Git。

驗證方式：

1. 填入 `VITE_GOOGLE_CLIENT_ID`。
2. 重啟 Vite dev server。
3. 開啟 `http://127.0.0.1:5173/Repair-Record-System/`。
4. 登入頁應顯示 Google 官方登入按鈕，而不是本機開發登入。

若看到 `Google 登入已略過：unknown_reason`，通常是 One Tap prompt 被瀏覽器或 Google 狀態略過。系統已改用 Google 官方登入按鈕 `renderButton()`，不再依賴 One Tap 自動提示。

---

## 2. GitHub Pages

目前專案固定部署到 GitHub Pages 專案頁：

```text
https://ghub-howard29.github.io/Repair-Record-System/
```

已設定：

```ts
base: '/Repair-Record-System/'
```

位置：

```text
vite.config.ts
```

注意：

- `base` 是專案設定一次，不需要每次部署手動設定。
- PWA manifest、favicon、service worker 已調整為跟隨 Vite base。

已建立 GitHub Actions workflow：

```text
.github/workflows/deploy-pages.yml
```

GitHub repository settings 需確認：

1. `Settings` → `Pages`
2. `Build and deployment`
3. `Source` 選 `GitHub Actions`

GitHub Actions secrets 部署前需確認：

```text
VITE_GOOGLE_CLIENT_ID
VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_APP_ID
VITE_GOOGLE_DRIVE_FOLDER_ID
```

若只驗證 Google OAuth 與靜態頁面，最低需要：

```text
VITE_GOOGLE_CLIENT_ID
```

Firebase / Drive 欄位已在本機 `.env` 完成；正式串接 Firestore 或 Drive 後，需同步補到 GitHub Actions secrets，避免部署版缺少設定。

部署驗證方式：

1. push 到 `main`。
2. 到 GitHub `Actions` 查看 `Deploy GitHub Pages` 是否成功。
3. 開啟 `https://ghub-howard29.github.io/Repair-Record-System/`。
4. 確認頁面、JS/CSS、manifest、Google 登入皆正常。

PWA / 網頁圖示：

```text
public/repair-system-icon-sheet.png
public/pwa-icon-512.png
public/pwa-icon-192.png
public/apple-touch-icon.png
public/favicon-32.png
```

其中 `repair-system-icon-sheet.png` 是原始功能圖示素材，其他檔案為裁切後輸出，已接到 manifest、favicon、Apple touch icon 與分享縮圖。

---

## 3. Firebase / Firestore

用途：

- 保存維修紀錄文字資料。
- 支援跨裝置即時查看。
- 後續替換目前 `localStorage` repository。

需要建立：

1. Firebase project。
2. Web app。
3. Firestore database。
4. Firebase Authentication 的 Google sign-in provider。
5. Firestore security rules。

`.env` 欄位：

```env
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
```

Firebase Console 取得位置：

```text
Project settings → General → Your apps → Web app → Firebase SDK snippet → Config
```

欄位對應：

| Firebase config | `.env` |
|-----------------|--------|
| `apiKey` | `VITE_FIREBASE_API_KEY` |
| `authDomain` | `VITE_FIREBASE_AUTH_DOMAIN` |
| `projectId` | `VITE_FIREBASE_PROJECT_ID` |
| `appId` | `VITE_FIREBASE_APP_ID` |
| `storageBucket` | 目前不使用，附件改由 Google Drive 保存 |
| `messagingSenderId` | 目前不使用 |
| `measurementId` | 目前不使用 |

建議 Firestore collection：

```text
repairRecords
```

文件 ID：

```text
RepairRecord.id
```

第一版資料權限建議：

- 必須透過 Firebase Auth 登入後才能讀寫。
- Google OAuth 登入成功後，前端會同步建立 Firebase Auth session，讓 Firestore Rules 可使用 `request.auth` 判斷身分。
- 第一版單一使用者，不做角色權限。
- 不提供刪除維修紀錄。

Rules 範本：

```text
firestore.rules
```

已由使用者在 Firebase Console 更新 Firestore Rules。後續若新增使用者，需將該 Google 帳號 Email 加入 allowlist；未列入清單的帳號不可讀寫維修紀錄。

待程式串接位置：

```text
src/services/firestoreRepairRecordService.ts
```

---

## 4. Google Drive API

用途：

- 保存照片附件。
- 後續透過後端代理替換目前附件 data URL 暫存。

需要建立：

1. 啟用 Google Drive API。
2. 建立專用資料夾，例如 `Repair Record Attachments`。
3. 取得資料夾 ID。
4. 後續建立 Service Account，並將資料夾分享給該 Service Account email。

`.env` 欄位：

```env
VITE_GOOGLE_DRIVE_FOLDER_ID=
VITE_GOOGLE_DRIVE_SCOPE=https://www.googleapis.com/auth/drive.file
```

目前保留的前端 scope：

```text
https://www.googleapis.com/auth/drive.file
```

待程式串接位置：

```text
src/services/googleDriveAttachmentService.ts
```

注意：

- 目前 Google 登入只取得登入身分。
- 不將 Google Service Account private key 放入前端 `.env` 或任何 `VITE_*` 欄位。
- 正式 Drive 上傳需由 Firebase Functions 或等效後端 endpoint 代理處理。
- 附件仍需遵守完成後鎖定，不可新增、更換、刪除。

---

## 5. 部署流程

Functions runtime 固定使用 Node.js 22；專案根目錄與 `functions/.nvmrc` 均已標記 `22`。部署前檢查會拒絕使用其他 major 版本，避免本機 Node 24 與雲端 runtime 行為不一致。

已建立：

- GitHub Actions 自動部署。
- 本機部署前檢查指令：

```powershell
npm run deploy:check
```

建議順序：

1. 先確認 `npm run build` 可成功。
2. push 到 `main` 觸發 GitHub Actions。
3. 部署後確認：
   - 頁面能開啟。
   - JS / CSS 資源路徑正常。
   - Google 登入來源已被允許。
   - PWA manifest 可讀取。

---

## 建議串接順序

1. 完成 Google OAuth 驗證。
2. 完成 GitHub Pages 部署驗證。
3. 建立 Firebase project 與 Firestore。
4. 串接 Firestore 維修文字同步。
5. 建立 Firebase Functions 或等效後端代理。
6. 透過 Service Account 串接 Google Drive 附件上傳。
7. 持續補齊 GitHub Actions secrets 與正式部署驗證紀錄。

---

## 目前交接狀態

已完成：

- Google OAuth 本機登入驗證。
- GitHub Pages 部署驗證。
- PWA / 網頁 icon 更新驗證。
- `.env` 基礎欄位整理。
- Firebase / Firestore `.env` 欄位已填入，並已依最新 Firebase / Google Cloud 專案統一設定。
- Firestore service 已串接。
- Firestore Rules 已由使用者更新到 Firebase Console。
- Google 登入、Firebase Auth session、Firestore 寫入與跨電腦雲端資料讀取已完成實機驗證。
- Google Drive folder ID 與 Drive scope 已填入。
- 已決定 Google Drive 正式附件上傳採 Service Account + 後端代理，不採前端直接持有服務帳戶權限。
- GitHub Actions 的 Google OAuth、Firebase 與 Drive 前端設定 Secrets 已補齊；GitHub Pages 正式站登入、Firestore 與附件上傳已驗收通過。
- 手機／直立窄版功能選單與寬版面工作台切換已驗收通過；附件操作已整合至新增／編輯維修單表單。

下一步：

1. 建立自動化測試，優先涵蓋維修規則、同步與 Firestore fallback。
2. 完成正式 PDF 與 `.xlsx` 匯出。
3. 完成手機、PWA、同步整合測試與 Release。
4. 視需要處理 Firebase bundle 過大警告。

下一次優先處理手機與桌面版面的功能頁內容、操作流程與視覺調整；雲端同步架構維持現況。

---

## Google Drive OAuth refresh token 資安決策

本專案不會把 Google Drive 的 OAuth refresh token 或 OAuth client secret 放入 Vite 前端 `.env`。

原因：

- 目前 app 是 GitHub Pages 靜態前端。
- 任何 `VITE_*` 變數在 build 後都可被瀏覽器端 JavaScript 讀到。
- 主要 Google Drive 帳號的 refresh token 必須視為後端 secret。
- 目前使用個人 Google Drive，不具備 Shared Drive；Service Account 不能擁有 Drive 檔案，因此改由主要 Drive 帳號的 OAuth refresh token 寫入資料夾。

建議 Drive 上傳架構：

```text
Frontend -> Firebase Function upload endpoint -> 主要帳號 OAuth refresh token -> Google Drive folder
```

前端後續只可新增非敏感設定，例如：

```env
VITE_ATTACHMENT_UPLOAD_ENABLED=true
```

後端專用設定才可存放敏感值：

```env
GOOGLE_DRIVE_FOLDER_ID=
GOOGLE_DRIVE_OAUTH_CLIENT_ID=
GOOGLE_DRIVE_OAUTH_CLIENT_SECRET=
GOOGLE_DRIVE_OAUTH_REFRESH_TOKEN=
ALLOWED_EMAILS=
```

實作注意事項：

1. 建立後端專用 OAuth Client，並由主要 Google Drive 帳號一次性授權 `drive.file` scope。
2. OAuth client secret 與 refresh token 存在 Firebase Functions Secret Manager。
3. `VITE_GOOGLE_DRIVE_FOLDER_ID` 只作為暫時或非敏感前端設定；後端 upload flow 完成後，Drive 寫入權限以後端設定為準。
4. 後端 upload flow 建立後，更新 `src/services/googleDriveAttachmentService.ts`，改呼叫後端 endpoint，不從瀏覽器直接呼叫 Google Drive 寫入 API。

### Functions 部署前設定

已建立 `functions/index.js` 的第二代 callable function：`uploadRepairAttachment`，部署區域為 `asia-east1`。部署前需設定以下 Firebase Functions secrets，且不得寫入前端 `.env`：

```text
GOOGLE_DRIVE_FOLDER_ID
GOOGLE_DRIVE_OAUTH_CLIENT_ID
GOOGLE_DRIVE_OAUTH_CLIENT_SECRET
GOOGLE_DRIVE_OAUTH_REFRESH_TOKEN
ALLOWED_EMAILS
```

`ALLOWED_EMAILS` 可使用逗號分隔的授權 Email，或 JSON 字串陣列（例如 `["haw1971@gmail.com"]`）。建立 refresh token 時，使用 `functions/tools/create-drive-refresh-token.mjs`，其 redirect URI 固定為 `http://127.0.0.1:53682/oauth2callback`；建立 OAuth Client 時必須使用 Desktop app 類型。完成 Functions 部署後，才將前端 `VITE_ATTACHMENT_UPLOAD_ENABLED` 設為 `true`。

GitHub Pages workflow 目前讀取的 Actions Secrets 名稱：

`VITE_GOOGLE_CLIENT_ID`、`VITE_FIREBASE_API_KEY`、`VITE_FIREBASE_AUTH_DOMAIN`、`VITE_FIREBASE_PROJECT_ID`、`VITE_FIREBASE_APP_ID`、`VITE_GOOGLE_DRIVE_FOLDER_ID`。
